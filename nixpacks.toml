# Заставляет Railway собирать Python-бэкенд (а не статический сайт из корня)
providers = ["python"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "python backend/bot.py"
